let arr;
let top = -1;

class stack {
    if(tops)
    push(x) {
    top++;
    arr[top] = x;
    }

    pop() {
        if(top = -1){ 
        console.log('Error : No elements to pop');
        return;
        }
        top--;
    }

    Top() {
        return A[top];
    }
}
const s = new stack();
s.push(3);